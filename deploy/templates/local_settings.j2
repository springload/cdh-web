# This file is exec'd from settings.py, so it has access to and can
# modify all the variables in settings.py.

# If this file is changed in development, the development server will
# have to be manually restarted because changes will not be noticed
# immediately.

DEBUG = {{ debug | default(False) }}

# Make these unique, and don't share it with anybody.
SECRET_KEY = "{{ secret_key }}"
NEVERCACHE_KEY = "{{ nevercache_key }}"

# override default media config so it is shared across deploys
MEDIA_ROOT = '/var/deploy/cdh-web/media'
MEDIA_URL = '/media/'

SHOW_TEST_WARNING = {{ test_waring | default(True) }}


DATABASES = {{ database_info }} 

CAS_SERVER_URL='https://fed.princeton.edu/cas/'

# configure ldap servers, search base, and filter
{{ pucas_settings }}

###################
# DEPLOY SETTINGS #
###################

# Domains for public site
ALLOWED_HOSTS = ["{{ allowed_host }}"]
